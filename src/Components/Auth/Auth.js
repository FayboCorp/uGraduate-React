/*
Example of post

const data = {
    password: this.props.password,
    username: this.props.username
}
axios.post(url, data)
    .then(response => {

    })
    .catch(error => {
        console.log(error);
    });
 */
